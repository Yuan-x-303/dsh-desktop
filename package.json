{
  "name": "dsh-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "One-click desktop shell for DeepSeek Harness (MVP)",
  "main": "dist/electron/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@deepseek-ai/dsh": "^0.1.0-rc.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^43.4.0",
    "typescript": "^5.7.3"
  }
}
